eureka:
  instance:
    prefer-ip-address: true

management:
  endpoints:
    enabled-by-default: true
  web:
    exposure:
      include: "*"
      exclude: "shutdown"
  endpoint:
    shutdown:
      enabled: false